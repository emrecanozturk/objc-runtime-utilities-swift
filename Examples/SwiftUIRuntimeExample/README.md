# SwiftUI Runtime Example

This folder contains a small SwiftUI/UIKit example that can be copied into an iOS app target.

The package itself is a SwiftPM library. The example is intentionally source-only so it can stay lightweight and easy to inspect.
