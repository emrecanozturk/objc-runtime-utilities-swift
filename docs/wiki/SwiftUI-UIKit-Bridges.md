# SwiftUI and UIKit Bridges

`ObjCRuntimeUtilitiesUI` contains small bridge examples.

`UIKitViewAdapter` wraps a UIKit view in SwiftUI and can attach a deallocation observer.

`RuntimeInspectorPanel` displays runtime metadata in a SwiftUI list.

These APIs are meant for diagnostics, examples, and narrow integration points rather than broad app architecture.
